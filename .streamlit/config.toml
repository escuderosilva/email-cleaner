[theme]
primaryColor = "#4F8DFD"
font = "sans serif"

[browser]
gatherUsageStats = false

[client]
toolbarMode = "minimal"

[server]
# Límite de subida (MB). Evita archivos gigantes que disparan la memoria.
maxUploadSize = 50
